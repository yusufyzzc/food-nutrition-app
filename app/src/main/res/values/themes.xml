<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.FoodNutritionApp" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorSecondary">@color/accent_color</item>

        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:colorBackground">@color/background_light</item>

        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Optional: apply CardView style globally -->
        <item name="cardViewStyle">@style/CardViewLightStyle</item>
    </style>

    <style name="CardViewLightStyle" parent="CardView">
        <item name="cardBackgroundColor">@color/card_background_light</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
    </style>

</resources>
