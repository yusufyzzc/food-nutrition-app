<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".FoodNutritionApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.FoodNutritionApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.NutritionDetailActivity"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".view.FavoritesActivity"
            android:parentActivityName=".MainActivity"
            android:label="My Favorites" />

        <activity
            android:name=".view.DailyTrackerActivity"
            android:parentActivityName=".MainActivity"
            android:label="Daily Tracker" />

        <!-- Comparison Activities -->
        <activity
            android:name=".view.FoodSelectionActivity"
            android:parentActivityName=".MainActivity"
            android:label="Select Foods" />

        <activity
            android:name=".view.ComparisonActivity"
            android:parentActivityName=".view.FoodSelectionActivity"
            android:label="Compare Foods" />
    </application>
</manifest>